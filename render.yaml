services:
  - type: web
    name: resvi-billing-app
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn "app:create_app()" --timeout 120 --workers 1
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.6"
